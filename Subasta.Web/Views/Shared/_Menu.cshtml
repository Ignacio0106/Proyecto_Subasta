<nav class="navbar navbar-expand-lg premium-navbar">
    <div class="container">

        <!-- LOGO -->
        <a class="navbar-brand fw-bold d-flex align-items-center" asp-controller="Home" asp-action="Index">
            <i class="bi bi-hammer me-2 text-warning"></i>
            <span class="brand-text text-white">Subasta</span>
        </a>

        <!-- BOTÓN RESPONSIVE -->
        <button class="navbar-toggler border-0" type="button"
                data-bs-toggle="collapse" data-bs-target="#navbarMain">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarMain">

            <!-- MENÚ -->
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">

                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle premium-link"
                       href="#" data-bs-toggle="dropdown">
                        <i class="bi bi-wrench me-1"></i>Mantenimiento
                    </a>
                    <ul class="dropdown-menu premium-dropdown">
                        <li><a class="dropdown-item" asp-controller="Usuario" asp-action="Index">Usuarios</a></li>
                        <li><a class="dropdown-item" asp-controller="Objeto" asp-action="Index">Objetos</a></li>
                    </ul>
                </li>

                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle premium-link"
                       href="#" data-bs-toggle="dropdown">
                        <i class="bi bi-hammer me-1"></i>Subastas
                    </a>
                    <ul class="dropdown-menu premium-dropdown">
                        <li><a class="dropdown-item" asp-controller="Subastaa" asp-action="Index">Todas</a></li>
                        <li><a class="dropdown-item" asp-controller="Subastaa" asp-action="Activas">Activas</a></li>
                        <li><a class="dropdown-item" asp-controller="Subastaa" asp-action="Finalizadas">Finalizadas</a></li>
                        <li><a class="dropdown-item" asp-controller="Subastaa" asp-action="Borradores">Borradores</a></li>
                    </ul>
                </li>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle premium-link"
                       href="#" data-bs-toggle="dropdown">
                        <i class="bi bi-credit-card me-1"></i>Pagos
                    </a>
                    <ul class="dropdown-menu premium-dropdown">
                        <li><a class="dropdown-item" asp-controller="Pago" asp-action="ListPagosByUser">Ver Pagos</a></li>
                    </ul>
                </li>
            </ul>

            <!-- USUARIO -->
            <ul class="navbar-nav ms-auto">
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle d-flex align-items-center premium-link"
                       href="#" data-bs-toggle="dropdown">

                        <!-- AVATAR -->
                        <div class="avatar-user me-2">
                            @(User.Identity?.Name != null
                                                        ? User.Identity.Name.Substring(0, 1).ToUpper()
                                                        : "?")
                        </div>

                        <!-- NOMBRE -->
                        <span class="d-none d-sm-inline fw-semibold">
                            @(User.Identity?.Name ?? "Invitado")
                        </span>
                    </a>

                    <ul class="dropdown-menu dropdown-menu-end premium-dropdown">

                        @if (User?.Identity?.IsAuthenticated == true)
                        {
                            <li>
                                <a class="dropdown-item" asp-controller="Login" asp-action="LogOff">
                                    <i class="bi bi-box-arrow-right me-2"></i> Salir
                                </a>
                            </li>
                        }
                        else
                        {
                            <li>
                                <a class="dropdown-item" asp-controller="Login" asp-action="Index">
                                    <i class="bi bi-box-arrow-in-right me-2"></i> Ingresar
                                </a>
                            </li>
                            <li>
                                <a class="dropdown-item" asp-controller="Login" asp-action="Registrar">
                                    <i class="bi bi-person-plus me-2"></i> Registrarse
                                </a>
                            </li>
                        }

                    </ul>
                </li>
            </ul>

        </div>
    </div>
</nav>
<style>
    /* NAVBAR */
    .premium-navbar {
        background: #0f0f0f;
        backdrop-filter: blur(12px);
        padding: 14px 0;
    }

    /* LOGO */
    .brand-text {
        font-family: 'Playfair Display', serif;
        font-size: 1.35rem;
        letter-spacing: 0.6px;
    }

    /* LINKS */
    .premium-link {
        position: relative;
        font-weight: 500;
        margin-right: 18px;
        color: #eaeaea;
        transition: all 0.3s ease;
    }

        .premium-link::after {
            content: "";
            position: absolute;
            left: 0;
            bottom: -5px;
            width: 0%;
            height: 2px;
            background: linear-gradient(90deg, #ffc107, #ff9800);
            transition: 0.3s ease;
        }

        .premium-link:hover::after {
            width: 100%;
        }

        .premium-link:hover {
            color: #ffc107 !important;
            text-shadow: 0 0 6px rgba(255,193,7,0.5);
        }

    /* DROPDOWN */
    .premium-dropdown {
        background: #121212;
        border: none;
        border-radius: 14px;
        padding: 10px;
        box-shadow: 0 12px 30px rgba(0,0,0,0.5);
    }

        .premium-dropdown .dropdown-item {
            color: #ccc;
            border-radius: 8px;
            padding: 8px 12px;
            transition: all 0.2s ease;
        }

            .premium-dropdown .dropdown-item:hover {
                background: rgba(255,193,7,0.1);
                color: #ffc107;
                transform: translateX(3px);
            }

    /* AVATAR */
    .avatar-user {
        width: 36px;
        height: 36px;
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        background: linear-gradient(135deg, #00c6ff, #0072ff);
        color: white;
        font-weight: 700;
        font-size: 14px;
        box-shadow: 0 0 10px rgba(0, 114, 255, 0.5);
        transition: all 0.25s ease;
    }

        .avatar-user:hover {
            transform: scale(1.1);
            box-shadow: 0 0 15px rgba(0, 114, 255, 0.8);
        }
</style>